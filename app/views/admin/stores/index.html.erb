<h1><%= t('store.stores')%></h1>
<%= link_to t('store.new'), new_admin_store_path %>
<ul>
    <% @stores.each do |store| %>
        <li>
            <%= store.id %>
            <a href="<%= admin_store_path(store) %>">
                <%= store.name %>
            </a>
        </li>
    <% end %>
</ul>
